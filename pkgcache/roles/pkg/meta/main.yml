---
galaxy_info:
  author: ClubCedille
  description: >-
    Configure pkg pour utiliser le cache/mirror interne Cedille (BasicAuth).
    Non validé avec un vrai client pkg — voir defaults/main.yaml.
  license: GPL-3.0-or-later
  min_ansible_version: "2.15"
  platforms:
    - name: FreeBSD
      versions:
        - all

dependencies: []
