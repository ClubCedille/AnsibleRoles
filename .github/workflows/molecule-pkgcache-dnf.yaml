---
name: Molecule - pkgcache.dnf

on:
  pull_request:
    paths:
      - "pkgcache/roles/dnf/**"
      - ".github/workflows/molecule-pkgcache-dnf.yaml"
  push:
    branches:
      - main
    paths:
      - "pkgcache/roles/dnf/**"
      - ".github/workflows/molecule-pkgcache-dnf.yaml"

concurrency:
  group: molecule-pkgcache-dnf-${{ github.workflow }}-${{ github.ref }}
  cancel-in-progress: true

jobs:
  molecule:
    name: Molecule test (pkgcache.dnf)
    runs-on: [self-hosted, docker-dind]
    defaults:
      run:
        working-directory: pkgcache/roles/dnf
    steps:
      - name: Checkout
        uses: actions/checkout@v5

      - name: Set up Python
        uses: actions/setup-python@v6
        with:
          python-version: "3.12"

      - name: Install Molecule and dependencies
        run: |
          python3 -m pip install --upgrade pip
          pip install \
            "ansible-core>=2.16.3" \
            molecule \
            "molecule-plugins[docker]" \
            docker \
            ansible-lint

      # Runner persistant : un résidu d'un run précédent fait échouer
      # ansible-galaxy avec "Cannot call rmtree on a symbolic link" (cf. AnsibleRoles#95).
      - name: Clean up any leftover cedille.pkgcache collection install
        run: rm -rf ~/.ansible/collections/ansible_collections/cedille/pkgcache

      - name: Install required Ansible collections
        run: |
          ansible-galaxy collection install -r molecule/default/requirements.yml

      - name: Molecule check (dry-run regression)
        run: molecule check
        env:
          PY_COLOR: "1"
          ANSIBLE_FORCE_COLOR: "1"

      - name: Molecule test
        run: molecule test
        env:
          PY_COLOR: "1"
          ANSIBLE_FORCE_COLOR: "1"

      - name: Clean up Molecule containers and images
        if: always()
        run: |
          molecule destroy || true
          docker rm -f pkgcache-dnf-fedora 2>/dev/null || true
