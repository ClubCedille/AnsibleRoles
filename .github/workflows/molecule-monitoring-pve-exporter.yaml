---
name: Molecule - monitoring.pve_exporter

on:
  pull_request:
    paths:
      - "monitoring/roles/pve_exporter/**"
      - ".github/workflows/molecule-monitoring-pve-exporter.yaml"
  push:
    branches:
      - main
    paths:
      - "monitoring/roles/pve_exporter/**"
      - ".github/workflows/molecule-monitoring-pve-exporter.yaml"

concurrency:
  group: molecule-monitoring-pve-exporter-${{ github.workflow }}-${{ github.ref }}
  cancel-in-progress: true

jobs:
  molecule:
    name: Molecule test (monitoring.pve_exporter)
    runs-on: [self-hosted, docker-dind]
    defaults:
      run:
        working-directory: monitoring/roles/pve_exporter
    steps:
      - name: Checkout
        uses: actions/checkout@v5

      - name: Set up Python
        uses: actions/setup-python@v6
        with:
          python-version: "3.12"

      - name: Install Molecule and dependencies
        run: |
          python3 -m pip install --upgrade pip
          pip install \
            "ansible-core>=2.16.3" \
            molecule \
            "molecule-plugins[docker]" \
            docker \
            ansible-lint

      # Runner persistant : un résidu d'un run précédent fait échouer
      # ansible-galaxy avec "Cannot call rmtree on a symbolic link" (cf. AnsibleRoles#95).
      - name: Clean up any leftover cedille.monitoring collection install
        run: rm -rf ~/.ansible/collections/ansible_collections/cedille/monitoring

      - name: Install required Ansible collections
        run: |
          ansible-galaxy collection install -r molecule/default/requirements.yml

      - name: Molecule check (dry-run regression)
        run: molecule check
        env:
          PY_COLOR: "1"
          ANSIBLE_FORCE_COLOR: "1"

      - name: Molecule test
        run: molecule test
        env:
          PY_COLOR: "1"
          ANSIBLE_FORCE_COLOR: "1"

      - name: Clean up Molecule containers and images
        if: always()
        run: |
          molecule destroy || true
          docker rm -f pve-exporter-ubuntu2404 2>/dev/null || true
