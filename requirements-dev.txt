# Dépendances de dev pour lancer les scénarios Molecule localement, alignées
# sur ce qu'installe la CI (.github/workflows/molecule-*.yaml).
ansible-core>=2.16.3
molecule
molecule-plugins[docker]
docker
ansible-lint
yamllint
