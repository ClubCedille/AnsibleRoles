---
- name: Reload systemd
  ansible.builtin.systemd:
    daemon_reload: true
  become: true

- name: Restart Alloy
  ansible.builtin.service:
    name: "{{ alloy_service.name }}"
    state: restarted
  become: true
