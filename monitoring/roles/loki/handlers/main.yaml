---

- name: Reload systemd
  ansible.builtin.systemd:
    daemon_reload: true
  become: true

- name: Restart Loki
  ansible.builtin.service:
    name: "{{ loki_service.name }}"
    state: restarted
  become: true
