auths:
{% for auth_name, auth in snmp_exporter_auths.items() %}
  {{ auth_name }}:
    community: {{ auth.community }}
    version: {{ auth.version }}
{% endfor %}

{{ lookup('file', 'snmp_modules.yml') }}
