---
- name: Reload systemd
  ansible.builtin.systemd:
    daemon_reload: true

- name: Restart ipmi_exporter
  ansible.builtin.systemd:
    name: "{{ ipmi_exporter_service.name }}"
    state: restarted
