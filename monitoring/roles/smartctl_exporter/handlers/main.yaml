---
- name: Reload systemd
  ansible.builtin.systemd:
    daemon_reload: true
  become: true

- name: Restart smartctl_exporter
  ansible.builtin.service:
    name: "{{ smartctl_exporter_service.name }}"
    state: restarted
  become: true
