---
- name: Reload systemd
  ansible.builtin.systemd:
    daemon_reload: true
  become: true

- name: Restart Prometheus
  ansible.builtin.service:
    name: "{{ prometheus_service.name }}"
    state: restarted
  become: true
