---
collections:
  # Requis par le driver docker de molecule-plugins lui-même (pas par le rôle).
  - name: ansible.posix
  # Requis par converge.yml pour provisionner une vraie PostgreSQL de test.
  - name: community.postgresql
